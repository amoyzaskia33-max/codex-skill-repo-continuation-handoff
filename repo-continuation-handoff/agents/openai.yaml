interface:
  display_name: "Repo Continuation"
  short_description: "Resume repos cleanly across sessions without goal drift"
  default_prompt: "Use $repo-continuation-handoff to recover or prepare repo continuity: pin the main goal, current scope, last completed work, next step, non-regression rules, and future-session handoff."
